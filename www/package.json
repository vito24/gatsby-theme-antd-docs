{
  "name": "gatsby-antd-docs",
  "description": "website",
  "version": "0.1.0",
  "author": "vito24 <sunhan.win@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "yarn clean && gatsby develop",
    "build": "yarn clean && gatsby build --prefix-paths",
    "deploy": "yarn build && gh-pages -d public",
    "serve": "gatsby serve --prefix-paths",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "antd": "^3.22.0",
    "gatsby": "^2.13.70",
    "gatsby-theme-antd-docs": "^0.1.3",
    "gh-pages": "^2.1.1",
    "prop-types": "^15.7.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "styled-components": "^4.3.2"
  }
}
